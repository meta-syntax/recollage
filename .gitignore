# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# ローカル実データ（個人の読書ハイライト・感想、Kindle引用＝著作権）。
# scripts/import-obsidian.ts で再生成する。コミット用のダミーは data/sample/ に置く。
data/mock

# DB バックアップ（npm run export:db の出力。実データを含むためコミットしない）
data/export

# 設計文書（ADR・負債台帳等）は意図的にこのリポジトリへ載せない（2026-07-07 確認）。
# 版管理・バックアップは docs/ 内の独立プライベートリポ（recollage-docs）が担う
docs/