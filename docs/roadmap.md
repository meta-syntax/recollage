# ロードマップ（フェーズ3以降のタスク台帳）

最終更新: 2026-07-05

聡太のアイデア出し（2026-07-05）を整理したもの。**順序と依存関係の整理であり、個別の設計判断はここでしない**（設計が必要なものは open-questions.md / 設計セッション → ADR へ）。

## 現在地

フェーズ3実装完了（モックフィード＋ADR-008スコア編成＋repository層）。検証仮説「自分のデータがビジュアルカードで並んだとき、実際にスクロールして読み返すか」の判定が手前にある。CMS・カテゴリ管理の設計は ADR-011 で確定済み（2026-07-05）。

## タスク一覧（依存順）

### 1. ~~詳細ページ — フェーズ3.5~~ → **実装完了（2026-07-05）**

`/entries/[id]`。カード・「つづきを読む」から遷移。閲覧記録は repository の `recordView` 経由で localStorage にオーバーレイ（フェーズ4で Supabase UPDATE に差し替え）。本文は parseBody（Markdown軽量サブセット）で整形。これにより Resurface の実地検証が可能になった。

### 2. CMS・入力フォーム — フェーズ4の本丸

「＋ 記録する」ボタンの遷移先。既製CMS不採用（ADR-006）＝自作。

- ~~着手前に設計セッションが必要~~ → **設計確定（ADR-011、2026-07-05）**: キャプチャモーダル（body のみ）／「整える」トースト＋誌面リマインダー／`/entries/[id]/edit` 一枚フォーム
- 依存: 書き込み系 repository（＝Supabase 移行・フェーズ4）とセット。フェーズ3の手応え待ち

### 3. カテゴリ追加・管理画面 — フェーズ4（CMSの一部）

- ~~設計論点: D&D 管理・sortOrder の順序表現~~ → **設計確定（ADR-011）**: `/categories` で D&D ツリー管理、整数連番のまま兄弟全件書き換え。実装時の論点はツリー D&D のライブラリ選定のみ
- 依存: 2 と同じく書き込み系。入力フォームより後でよい（現状カテゴリはインポータの固定ツリー）

### 4. MCP サーバー提供 — 超拡張（フェーズ5+のアイデア置き場）

AI と壁打ちした結果を、MCP 経由で Recollage にエントリとして書き込む。

- 構想: Recollage が MCP サーバーを公開し、壁打ちセッションの学び・結論を `create_entry` ツールで直接記録 → フィードの「出会い直し」対象になる
- 効いてくる資産: repository 抽象（ADR-001）と API レイヤー折衷（ADR-004）が入り口を規定済み。書き捨てエントリへの LLM 自動補完（open-questions 先送り）と相性が良い
- 依存: 書き込み系 API（フェーズ4完了後）。それまでアイデアとして凍結

## 手前に挟むべき小タスク

- ~~コールドアーカイブ問題のパラメータ方針決め~~ → **検証中に確認へ整理（2026-07-05）**。初期誌面は乱択だが閲覧で自己回復する。不満が出たらフォールバック項を判断（open-questions 参照）
- 検証仮説の判定（1〜2週間、実データで日常使用）— フェーズ4 GO/NO-GO の入力。初期誌面は乱択と認識した上で、Resurface の評価は閲覧履歴が溜まってから
